# aventure-mcp

Configure the public aVenture MCP server.

- Install: `npm install -g @aventurevc/mcp-server`
- Run: `aventure-mcp-server`
- Read the docs at https://github.com/aventurevc/aventure-mcp
- The server exposes only public operations; use `aventure help` for the CLI command catalog.
